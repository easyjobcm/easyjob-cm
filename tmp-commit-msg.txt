feat(momo): T6 — preuve de possession OTP + validation admin des comptes Mobile Money

- Migration : cycle de vie MoMo sur candidate_profiles (momo_account_name,
  momo_otp_status none|awaiting|verified|rejected, momo_reject_reason,
  momo_verified_by/at), table momo_otp avec RLS deny-by-default, trigger de
  protection des 6 colonnes sensibles (easyjob.system_update) et 4 RPC
  SECURITY DEFINER (candidate_update_momo, momo_issue_otp, momo_verify_otp,
  apply_momo_verification) ; nouvelle valeur 'momo_status' sur
  notification_type.
- lib/momo-otp.ts : code 6 chiffres CSPRNG, hash SHA-256 (le code en clair
  n'est jamais persisté), TTL 10 min, envoi SMS Twilio REST natif avec
  fallback sandbox (log côté serveur) et quotas anti-abus repris de
  check_sms_send_log.
- Routes candidat : PUT /api/profile/payment passe par le RPC (changement de
  numéro = réinitialisation complète), POST /momo/otp (400 sans numéro,
  quota 429), POST /momo/otp/verify (numéro transmis comparé au numéro
  enregistré, 3 échecs = preuve rejetée 'otp_max_attempts').
- Route admin GET/POST /api/admin/momo : listing (numéro en clair pour la
  confrontation CNI), approve/reject transactionnel dans le RPC avec preuve
  OTP préalable obligatoire (400 otp_proof_required), notification
  'momo_status' et audit approve_momo/reject_momo portés par l'admin réel.
- UI : page /profile/payment réécrite (machine d'état OTP, carte « Vérifié »
  seulement après l'admin, numéro masqué, ré-émission 30 s), onboarding
  step 4 + champ « Nom du compte » optionnel (hint CNI) et garde-fou
  paymentSchema dans saveProgress.
- Tests : 27 tests vitest (lib momo + paymentSchema/momoOtpSchema/
  momoModerateSchema) et preuve E2E scripts/proof-profile-momo.ts
  (19 assertions sur session réelle, sandbox SMS).
- SRS §6.6 / §8.1 / §11.5 mis à jour (état implémenté T6).

Gates : vitest 141/141 · tsc · eslint 0 warning · prettier · build ·
preuve E2E 19/19.

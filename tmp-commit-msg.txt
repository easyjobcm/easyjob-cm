feat(admin): T8.5 — mises à jour de profil : annuler/supprimer + initiation admin

Surface admin du modèle T2 (SRS §5.1.1) « l'admin initie, le candidat
exécute » (pas de sémantique approuver/refuser) :

- Page /admin/update-requests (SWR) : 4 sections avec compteurs
  (En attente / Exécutées / Annulées / Toutes), chaque ligne porte le
  candidat, les groupes de champs, le motif, la date + l'initiateur
  (e-mail résolu service_role). Lecture = 3 grades ; mutations
  (annuler / purger / créer) = admin_ops + admin_founder.
- GET /api/admin/profile-update-requests (nouveau, session admin : la
  table a une policy SELECT admin explicite — contrairement à users)
  + filtres ?status= / ?candidate_id=.
- PATCH /api/admin/profile-update-requests/[id] (nouveau) : annuler,
  transition UNIQUEMENT pending → cancelled (la RLS admin est
  permissive, la route est l'unique source de vérité) ; idempotente si
  déjà cancelled, 409 invalid_transition sinon. Audit
  cancel_profile_update_request + notification au candidat.
- DELETE (même route) : purge des lignes clôturées (done/cancelled)
  UNIQUEMENT — jamais une pending (409 request_pending : elle
  déverrouille les champs du candidat en cours d'édition). L'audit
  delete_profile_update_request est écrit AVANT le delete (il survit).
- Initiation admin 2 entrées (décision produit) : bouton « Demander
  une mise à jour » ouvrant UpdateRequestModal (composant partagé)
  (a) sur la page liste (recherche candidate SWR) et (b) sur le profil
  candidat /admin/candidates/[id] — nouvelle section « Mises à jour de
  profil » (liste ?candidate_id=) + modal épinglé au candidat affiché.
  Le POST (existant depuis T2) crée la ligne pending + audit +
  notification. Clôture done reste automatique côté serveur
  (completePendingRequests).
- i18n fr/en : bloc admin.updateRequests + 4 clés
  admin.candidateProfile (section profil) — parité FR/EN garantie par
  les types.

Aucun item de navigation basse (accès dashboard + profil candidat ;
/admin/jobs et /admin/settings arrivent en T8.6/T8.8). SRS §6.12
mis à jour. Preuve E2E scripts/proof-admin-update-requests.ts 38/38
(sessions réelles : page SSR + filtres + POST/PATCH/DELETE : rôles,
transitions, idempotence, audits, notifications, purge + 0 DDL —
diff-types inchangé 7d81e1bd…).
